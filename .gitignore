# OS
.DS_Store
Thumbs.db

# Secrets (Notion / local env)
.env
**/.env
!**/.env.example

# Local Obsidian state (regenerated per device)
.obsidian/workspace.json
.obsidian/workspace.mobile.json
.obsidian/workspaces.json

# Optional: uncomment if you don't want plugin code in the repo
# .obsidian/plugins/
# .obsidian/themes/

# Agent / LLM: long research, scratch dumps, exported answers (ephemeral; not project source)
/temp/
